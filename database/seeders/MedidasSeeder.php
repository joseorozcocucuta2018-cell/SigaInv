<?php

namespace Database\Seeders;

use Illuminate\Database\Seeder;

/**
 * MedidasSeeder — vacío / deprecado
 *
 * No se usa en DatabaseSeeder. Las unidades de medida se cargan con
 * UnidadesMedidaSeeder. Este archivo se mantiene por compatibilidad
 * o puede eliminarse si no se referencia en ningún script.
 */
class MedidasSeeder extends Seeder
{
    public function run(): void
    {
        // Sin contenido — usar UnidadesMedidaSeeder para unidades de medida.
    }
}
